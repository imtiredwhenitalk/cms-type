const { createAppRouteCode } = require("next/dist/build/webpack/loaders/next-app-loader/create-app-route-code");

function App() {
    getComputedStyle,createAppRouteCode(App)
}

function start() {
    start.apply(App, arguments)
} 

function getComputedStyle() {
    getComputedStyle.apply(App, arguments)
}

class app_started {
    App_started = app_started
    app_started(listening=5000)
}

module.exports = {
    App,
    start,
    getComputedStyle
}

